enum Role {
    CUSTOMER
    BUYER
    MODERATOR
    ADMIN
}

enum UserStatus {
    ACTIVE
    INACTIVE
    SUSPENDED
    BLOCKED
    DELETED
}

enum Gender {
    MALE
    FEMALE
    OTHER
}

enum AuthProvider {
    GOOGLE
    CREDENTIAL
}

enum TrustRuleStatus {
    VERIFICATION
    ACTIVE
    INACTIVE
    REPORT
}
